name: quito_transport
version: "1.0.0"
config-version: 2

profile: quito_transport

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  quito_transport:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
