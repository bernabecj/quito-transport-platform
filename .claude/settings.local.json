{
  "permissions": {
    "allow": [
      "PowerShell(if \\(-not $?\\) { \"Directory does not exist or is empty\" })",
      "Bash(poetry add *)",
      "Bash(poetry install *)",
      "Bash(poetry run *)",
      "Bash(poetry lock *)"
    ]
  }
}
